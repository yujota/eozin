{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:rust": "cd ../viewer && wasm-pack build --target web --release --out-name eozin --out-dir ../web/pkg/",
    "build:rust:dev": "cd ../viewer && wasm-pack build --target web --debug --out-name eozin --out-dir ../web/pkg/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.10"
  }
}
