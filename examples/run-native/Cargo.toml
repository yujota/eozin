[package]
name = "run-native"
version.workspace = true
authors.workspace = true
license.workspace = true
edition.workspace = true

[dependencies]
eozin = { path = "../../", features = ["std-io"], default-features = false }
reqwest = { version = "^0.13.2", features = ["blocking"] }
